name: Terragrunt Deploy

on:
  push:
    paths:
      - "environments/*/*/terragrunt.hcl"

jobs:
  detect-changes:
    runs-on: ubuntu-latest
    outputs:
      changed_dirs: ${{ steps.detect_changes.outputs.changed_dirs }}
    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: Detect changed directories
        id: detect_changes
        run: |
          changed_files=$(git diff --name-only ${{ github.event.before }} ${{ github.sha }} | grep 'terragrunt.hcl' || true)

          changed_dirs=$(echo "$changed_files" | xargs -n1 dirname | sort -u)

          changed_dirs_json=$(printf '[%s]' "$(echo "$changed_dirs" | sed 's/$/","/;s/^/"/;s/","$//')")

          echo "changed_dirs=$changed_dirs_json" >> $GITHUB_OUTPUT

  terragrunt-apply:
    runs-on: ubuntu-latest
    needs: detect-changes
    strategy:
      fail-fast: false
      matrix:
        dir: ${{ fromJson(needs.detect-changes.outputs.changed_dirs) }}
    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: Terragrunt
        run: |
          echo ${{ matrix.dir }}
          echo "TERRAGRUNT"
